# Sound mix — midlife-movement-brain

Words win the mix. **Music pick: B — Paper hush.**

## Status

| File | Role | Notes |
|---|---|---|
| `public/audio/vo.mp3` | Voiceover | Daniel (macOS `say`), ~58.3s, loudnorm |
| `public/audio/music.mp3` | Underscore | Soft sparse pads + pink hush, 65s |
| `public/audio/sfx-stamp.mp3` | Stamp | Soft thump |
| `public/audio/sfx-tick.mp3` | Tick | Soft click |
| `public/audio/sfx-whoosh.mp3` | Whoosh | Soft paper whoosh (rare) |

`Main` mix buses default **on**. Tune `voLevel` / `musicLevel` / SFX in Studio.

## Music (locked)

**B — Paper hush** — sparse soft pad + quiet room tone under VO.

## SFX hits (wired)

Sparse stamp / tick / whoosh cues on `Main` (`STAMP_HITS` / `TICK_HITS` / `WHOOSH_HITS`). Cut any that announce themselves.

## Review

Listen on `Main` without watching first. Reply **Approve** to lock the mix and move to render, or **Revise: …** (levels, VO voice, music, SFX).
